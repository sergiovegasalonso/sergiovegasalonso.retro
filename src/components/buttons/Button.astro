---
const { id, text } = Astro.props;
---

<button
  id={id}
  class="flex cursor-pointer border-2 px-2 py-1 lg:px-3 lg:py-2"
  aria-label={text}
>
  <slot />
  <span class="hidden lg:ml-1 lg:inline">{text}</span>
</button>
